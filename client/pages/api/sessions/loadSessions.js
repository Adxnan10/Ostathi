import db_model from "../../../models/db_model"

export default async function handler(req, res) {

  console.log(req.body)
  // const loadedSessions = await db_model.getAllRequestedSessions('o', 's')
  // res.send(loadedSessions)

}